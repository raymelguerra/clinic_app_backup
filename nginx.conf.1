events {
    worker_connections 1024;
}

http {
  server {
    listen 80;
    server_name localhost;

    location ^contractor(\/.*)? {
       proxy_pass http://clinicapp.contractor$request_uri;
    }
#^b(\/.*)?
  }
}