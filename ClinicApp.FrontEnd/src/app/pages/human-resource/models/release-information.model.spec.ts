import { ReleaseInformation } from './release-information.model';

describe('ReleaseInformation', () => {
  it('should create an instance', () => {
    expect(new ReleaseInformation()).toBeTruthy();
  });
});
