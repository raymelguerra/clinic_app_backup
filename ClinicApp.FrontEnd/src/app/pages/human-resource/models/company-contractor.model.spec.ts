import { CompanyContractor } from './company-contractor.model';

describe('CompanyContractor', () => {
  it('should create an instance', () => {
    expect(new CompanyContractor()).toBeTruthy();
  });
});
